import { AsideToggleDirective } from './aside-toggle.directive';

describe('AsideToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideToggleDirective();
    expect(directive).toBeTruthy();
  });
});
